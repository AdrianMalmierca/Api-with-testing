version: "3.9"

services:
  app:
    build: .
    container_name: movie-api
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      DB_PATH: /app/data/database.sqlite
    volumes:
      - sqlite-data:/app/data

volumes:
  sqlite-data: